{
  "name": "linker-agent-vercel-root",
  "private": true,
  "type": "module",
  "scripts": {
    "install:app": "npm install --include=optional --prefix linker-agent11",
    "build": "npm run build --prefix linker-agent11",
    "vercel-build": "npm run build --prefix linker-agent11",
    "dev": "npm run dev --prefix linker-agent11",
    "start": "npm run start --prefix linker-agent11",
    "preview": "npm run preview --prefix linker-agent11",
    "prisma:generate": "npx prisma generate --schema=linker-agent11/prisma/schema.prisma"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {},
  "devDependencies": {}
}
