{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && npx prisma generate && npx prisma db push && tsc -b"
  },
  "deploy": {
    "startCommand": "node server/index.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
